"use strict";

angular
    .module('prjIdvApp')
    .controller('PessoasController', ['$rootScope', '$scope', '$timeout', '$q',
        function($rootScope, $scope, $timeout, $q) {
            var self = this;


            ;(function() {

            }());
        }]);
